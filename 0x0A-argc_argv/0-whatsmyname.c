#include "main.h"

/**
 * main -  program that prints its name.
 * @argc: argument counter
 * @argv: argument
 * Return: 0
 */

int main(int argc, char *argv[])
{
	argc = 0;
	printf("%s \n", argv[argc]);

	return (0);
}
